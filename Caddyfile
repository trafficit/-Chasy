# {$SITE_ADDRESS} is taken from .env:
#   production : worklog.example.com
#   local test : :80
{$SITE_ADDRESS} {
	encode gzip
	reverse_proxy app:8000 {
		# real client IP for rate limiting (Caddy overwrites -> not spoofable)
		header_up X-Real-IP {remote_host}
	}
}
